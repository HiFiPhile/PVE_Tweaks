--- /usr/share/perl5/PVE/QemuServer.pm	2019-01-10 10:18:43.370386455 +0100
+++ ./QemuServer.pm	2019-01-11 14:31:48.462226510 +0100
@@ -607,6 +607,11 @@
 	default => "1 (autogenerated)",
 	optional => 1,
     },
+    postcommand => {
+    optional => 1,
+    type => 'string',
+    description => "Command to be executed after VM start.",
+    },
 };
 
 my $confdesc_cloudinit = {
@@ -5293,6 +5298,11 @@
 		    property => "guest-stats-polling-interval",
 		    value => 2) if (!defined($conf->{balloon}) || $conf->{balloon});
 
+	if (my $postcommand = $conf->{postcommand}) {
+		print "Execute post start command: $postcommand\n";
+		run_command($postcommand);
+	}
+	
     });
 }
 
